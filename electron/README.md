# Electron wrapper

Entry points:
- `electron/main.js`: main process, creates the desktop window
- `electron/preload.js`: safe bridge for future native integrations

The Angular build output is expected at `dist/text-calculator/`.
